{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "rm -rf node_modules && npm install && npm run build && npm install -g serve"
  },
  "deploy": {
    "startCommand": "serve -s dist -p $PORT",
    "restartPolicyType": "ON_FAILURE"
  }
}
