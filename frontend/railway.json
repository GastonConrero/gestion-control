{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build && npm install -g serve"
  },
  "deploy": {
    "startCommand": "serve -s dist -p 8080",
    "restartPolicyType": "ON_FAILURE"
  }
}
