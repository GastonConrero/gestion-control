{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build"
  },
  "deploy": {
    "startCommand": "caddy file-server --root dist --listen :$PORT",
    "restartPolicyType": "ON_FAILURE"
  }
}
