[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "python311Packages.virtualenv", "gcc"]

[phases.install]
cmds = [
  "python3 -m venv /opt/venv2",
  "/opt/venv2/bin/pip install -r backend/requirements.txt"
]

[phases.build]
cmds = []

[start]
cmd = "cd backend && /opt/venv2/bin/uvicorn app.main:app --host 0.0.0.0 --port $PORT"
