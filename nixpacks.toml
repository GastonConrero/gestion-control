[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "gcc"]

[phases.install]
cmds = ["pip3 install -r backend/requirements.txt --break-system-packages"]

[phases.build]
cmds = []

[start]
cmd = "cd backend && python3 -m uvicorn app.main:app --host 0.0.0.0 --port 8000"
