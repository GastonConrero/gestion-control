{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm ci && npm run build && npm install express"
  },
  "deploy": {
    "startCommand": "node server.js",
    "restartPolicyType": "ON_FAILURE"
  }
}
